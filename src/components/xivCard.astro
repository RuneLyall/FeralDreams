---
const { job, icon, role, xp, txp } = Astro.props;
---

<div class="flex items-center space-x-4">
    <img src={ icon } alt = { job } class="w-10 h-10 object-cover"/> 
    <div class="justify-self-center">{ job }</div>

</div>